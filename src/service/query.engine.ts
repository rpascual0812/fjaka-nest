export class QueryEngine {
    // chain(employee: any) {
    //     this.query = "select * from timelogs";
    //     return this;
    // }

    // async id(id): Promise <any> {
    //     let timelogs = await this.timelogsRepository.query(this.query + ' where id = $1', [id]);
    //     return timelogs;
    // }
}
